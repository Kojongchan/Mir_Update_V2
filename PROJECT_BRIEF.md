# MIR_SMART 자체 3D 뷰어 — 프로젝트 킥오프 브리프
> 이 문서는 Claude Code가 이 프로젝트를 이어받아 진행하기 위한 **단일 진실 소스(single source of truth)**다.
> 레포 루트에 `PROJECT_BRIEF.md`로 두고, 핵심 규칙은 `CLAUDE.md`에도 복사한다.
> 작성 시점의 모든 아키텍처 결정과 그 **근거·기각된 대안·미해결 리스크**를 포함한다. 결정을 바꿀 때는 근거를 이 문서에 갱신한다.
---
## 0. 한 줄 요약
Autodesk ACC 뷰어(렉 심함)를 대체하는 **사내 전용 웹 기반 3D BIM 디지털트윈 뷰어**를 자체 개발한다. rvt·dwg·ifc·fbx를 업로드하면 좌표 포함 3D로 렌더링하고, MIR_SMART의 전 기능(물량/진도/RFI)과 오브젝트 단위로 연동하며, 지도·위성 배경 위에서 위치 파악이 가능해야 한다. **최우선 가치는 "가벼움"** — 이게 깨지면 나머지가 무의미하다(ACC를 버리는 이유가 렉이므로).
---
## 1. 본질 & 우선순위 (절대 놓치지 말 것)
사용자가 직접 압축한 요구 5가지. **순서가 곧 우선순위다.**
1. **가벼운 자체 3D 뷰** — ACC 뷰어 렉 탈출. 이게 시작점이자 본질.
2. **뷰 퀄리티** — 실사감(디지털트윈처럼 실제같은 3D 뷰).
3. **다포맷 업로드 → 3D** — rvt / dwg / ifc / fbx, **좌표 포함**.
4. **MIR_SMART 전 기능 연동** — 물량(QTO)·진도 대시보드·RFI 전부. 트윈 b형(아래 §6).
5. **지도 기능** — 위성/지도 배경, 위치 파악.
> **판단 원칙:** 1번(가벼움)과 5번(지도)은 물리적으로 상충한다. 한 화면에 지도+풀디테일 구조물+지형+도면을 다 켜면 ACC보다 무거워진다. → **모드 분리**로 해결(§5). "다 항상 보이게"를 고집하면 가벼움을 포기해야 한다는 걸 명심.
---
## 2. 배경 & 문제 정의 (왜 자체개발인가)
### 2.1 ACC(APS) 뷰어를 버리는 이유
- ACC가 쓰는 APS(구 Forge) Viewer는 SVF2 스트리밍 기반으로 잘 최적화돼 있으나, **실사용 시 렉이 심함**(사용자 직접 확인). 원인 후보: 대형 모델 + 클라우드→한국 네트워크 지연 + 벤더 락인.
- 벤더 락인·라이선스 비용 회피.
### 2.2 과거 That Open(뎃오픈) 시도가 실패한 진짜 원인 — **재발 방지 핵심**
사용자가 논문 작업 때 That Open(web-ifc)을 MIR_SMART에 붙였으나 **전체 용량 파일을 못 받고 터짐**. 원인은 라이브러리 한계가 **아니라** 두 가지 아키텍처 실수로 추정:
1. **클라이언트(브라우저) 런타임 IFC 파싱** → web-ifc가 IFC를 브라우저 WASM 메모리에 통째로 올려 파싱. 브라우저 메모리 상한(~2–4GB)에서 대용량이 죽음.
   - That Open **공식 문서도** 런타임 IFC 로딩은 프로덕션에 너무 느리다고 명시. 권장: 서버에서 IFC→Fragments **1회 사전변환** 후 그 파일만 로딩(2GB IFC STEP → 80MB Fragments).
2. **float32 좌표 지터** → 우리 데이터는 중부/동부원점 실좌표(20만/60만대). single-precision(float32) 엔진(Three.js/Fragments 기본)은 이 큰 숫자에서 정밀도가 뭉개져 모델 떨림·z-fighting 발생.
> **결론: "가벼움"은 엔진 배지가 아니라 (a) 서버측 사전변환 + (b) 더블 프리시전 엔진에서 나온다. 이 두 가지를 반드시 지킨다.**
---
## 3. 확정 아키텍처 — 듀얼 엔진 / 모드 전환
두 요구(정밀 BIM vs 지도/위성)가 한 엔진으로 동시에 최적화되지 않으므로, **목적별 모드 전환**으로 가벼움을 지킨다.
### 3.1 BIM 검토 모드 (주력, 정밀 작업)
- **엔진: xeokit SDK**
- 용도: 구조물 풀디테일 렌더, 속성/물량/진도/RFI 접근, 정밀 형상 검토.
- 지도 배경 끔(또는 단순 지형만).
- 여기가 MIR_SMART 기능이 다 붙는 곳.
### 3.2 지도 모드 (위치 파악, 광역 맥락)
- **엔진: Cesium (베이스맵) + 경량 구조물 프록시**
- 용도: 위성/지도 배경 토글, 프로젝트 위치, 주변 맥락, CAD 도면 오버레이(구글어스식).
- 구조물은 저밀도 3D Tiles 프록시(정밀 속성 불필요).
### 3.3 두 모드 연결
- 카메라 위치 공유 → 모드 전환 시 시점 유지.
- **한 씬에 전부 욱여넣지 않는다.** 이게 가벼움의 핵심.
### 3.4 엔진 선택 근거 (기각된 대안 포함)
- **xeokit 채택 이유:**
  - **더블 프리시전(float64) 실좌표 네이티브** → 중부원점 60만대 좌표에서 지터 없음. (That Open 실패의 좌표 원인을 근본 해결.)
  - DTX(데이터텍스처) 모드 + LOD + 컬링 → 수십만 오브젝트 60fps(가벼움).
  - BIM 특화 속성 API(GUID·PropertySet 접근) → MIR_SMART 연동에 유리.
  - **사내 전용 확정** → xeokit의 AGPLv3 라이선스 무관(배포 안 하므로 소스공개 의무 미발생). *만약 향후 외부 배포/SaaS화하면 법무 재검토 필수.*
  - 토목·교량·대형 인프라·GIS 결합 디지털트윈에 실전 검증됨.
- **Fragments(That Open, MIT) 기각:** float32 기반이라 60만대 좌표에서 re-center 처리를 직접 넣어야 함. 라이선스는 더 자유롭지만(MIT) 지터 문제로 우리 데이터엔 xeokit이 우위.
- **Cesium을 지형 스트리밍용으로는 기각:** 전 구간 지형이 ~24MB(§4)라 스트리밍 엔진 불필요. **단 지도/위성 배경·CAD 오버레이 요구 때문에 "지도 모드 베이스"로는 채택**(용도가 다름).
- **순수 web-ifc + Three.js 직접구축 기각:** 트리·컬링·LOD·속성패널 전부 재발명. 유지불가.
- **Unity/Unreal 기각:** 웹 BIM 대시보드엔 오버킬.
---
## 4. 데이터 실측 (설계 근거가 된 실제 값)
InfraWorks 지형 샘플 `ground_수정` 폴더 분석:
- `ground_수정.fbx` (지오메트리): **14.6MB**
- `ground_수정.fbm` (임베드 텍스처 = 오쏘 드래이핑): **9.7MB, 파일 4개**
- `ground_수정.nwc` (Navisworks 캐시): 22.6MB — **사용 안 함** (지형은 FBX 라인)
- `ground_수정.pos` (지오레퍼런싱): 1KB — **핵심 자산**(§5)
**이 FBX는 전 구간 통합 지형이다** (일부 구획 아님). 즉 전 구간 = 지오메트리 14MB + 오쏘 10MB ≈ **24MB 수준**.
### 판단
- 전 구간이 수십 MB급 → **xeokit 단독으로 충분. 지형 스트리밍용 Cesium 3D Tiles 불필요.**
- 단, 프로젝트마다 편차 커서 오쏘가 "몇십배"(수백MB~GB) 되는 케이스 있음 → 인제스트에서 **오쏘 용량 기준 분기**(§8) 필요.
- **오쏘 텍스처가 20km 트윈 성능의 8할을 결정한다** (지형 메시는 가벼움). → KTX2/Basis 압축 + 밉맵 필수.
---
## 5. 좌표계 처리 — **완결된 설계 (가장 중요한 파트)**
### 5.1 핵심 사실: "좌표값 있음" ≠ "좌표계 정의 있음"
- **Civil 3D (DWG):** 좌표계를 안다(도면 설정에 EPSG 내장). 넷 중 유일.
- **Revit (RVT):** 좌표값은 있으나 어느 원점계인지 파일이 모름(Shared Coordinates는 상대 위치만). → **사람이 프로젝트당 1회 지정 필요.**
- **FBX (InfraWorks):** FBX 본체 좌표는 로컬 재센터링 값일 수 있고, 실세계 CRS·원점은 옆의 `.pos`에 있음.
- **IFC:** IFC4.3의 `IfcMapConversion`/`IfcProjectedCRS`로 담을 수 있으나 Revit export 시 대개 비어 있음.
### 5.2 `.pos` 파일 파싱 규칙 (확정)
`.pos`는 GeoJSON 형태. InfraWorks가 FBX와 함께 내보냄. 실제 샘플 내용:
```
crs.properties.name = PROJCS["KOREA_GRS80_127TM", ...
  central_meridian 127, false_northing 600000, false_easting 200000,
  scale_factor 1.0, latitude_of_origin 38, GRS1980 ...]
coordinates = [0, 0, 0]   // ← 오프셋 비어 있음 (주의)
```
**파싱 로직:**
1. **이름 문자열 무시.** `KOREA_GRS80_127TM`은 Civil 3D에서 만든 사용자 정의 좌표계 이름일 뿐(사용자 확인). EPSG 판별에 쓰지 않는다.
2. **`central_meridian` + `false_northing`으로 EPSG 확정:**
   | central_meridian | 원점 | EPSG |
   |---|---|---|
   | 125 | 서부 | 5185 |
   | **127** | **중부** | **5186** (false_northing 600000) |
   | 129 | 동부 | 5187 |
   | 131 | 동해 | 5188 |
   - 주의: `false_northing 500000`이면 구 좌표계(5174 계열)일 수 있으니 northing 값도 검사.
3. **`coordinates` 오프셋 검사:** 샘플은 `[0,0,0]`(미기재). 이 경우 FBX 정점 좌표계로 이중 확인:
   - **본 프로젝트 확인 결과: FBX 정점이 실좌표(20만/60만대)임 → 시나리오 1.**
   - 시나리오 1: 정점이 이미 실세계 중부원점 좌표 → **오프셋 이동 불필요. `.pos`는 CRS 태그로만 사용.**
   - (다른 프로젝트에서 정점이 0 근처면 → 시나리오 2: 프로젝트 생성 UI에서 실세계 원점 1개 입력받아야 함. 방어 로직 유지.)
### 5.3 자동 정합 (이 프로젝트 유형의 이점)
- 지형(FBX)·구조물(Revit→IFC)·선형(Civil3D)이 **모두 동일 중부/동부 평면 실좌표**로 맞춰지면 → **오프셋 계산·정렬 로직 없이 한 씬에 얹으면 자동 정합.**
- 지터는 **엔진(xeokit 더블 프리시전)이 담당** → 60만대 좌표 그대로 넣어도 안 흔들림.
### 5.4 좌표 변환은 "지도 모드에서만"
- **BIM 검토 모드(xeokit):** 평면 실좌표(TM) 그대로. 변환 없음.
- **지도 모드(Cesium):** Cesium이 WGS84/ECEF 기반이라 **5186/5187 → WGS84 재투영 필요**. `pyproj`(PROJC) 사용, EPSG 코드만 넘기면 한 줄. **재투영을 지도 모드에 국한**해 복잡도 최소화.
- Revit 좌표 자동판별은 신뢰도 낮음 → **자동화 금지.** 프로젝트 생성 UI에 "좌표계 선택(중부/동부/서부/동해)" 드롭다운 + FBX(.pos)·DWG 자동 + Revit 사용자 확인.
### 5.5 Navisworks 관련 정정 (오해 방지)
- Navisworks에서 FBX 로드 시 좌표가 살아있는 건 **Navisworks가 옆의 `.pos`를 자동으로 읽기 때문.** FBX 본체가 CRS를 아는 게 아님.
- → **우리 뷰어도 `.pos`만 파싱하면 Navisworks와 동일 결과 재현 가능. Navisworks는 자동화 병목이므로 인제스트 경로에서 제외.**
---
## 6. 포맷별 인제스트 파이프라인
### 6.1 포맷 리딩의 구조적 벽 (인식 필수)
- **rvt·nwd·dwg는 오토데스크 소유 폐쇄 포맷.** 순수 오픈소스로 풀 피델리티 리딩 불가. ACC가 "다 되는" 이유는 오토데스크가 자기 포맷을 자기가 읽기 때문.
- 우회로는 항상 **변환 레이어**를 거침. rvt/dwg 오프라인 변환 도구(예: DataDrivenConstruction)는 내부적으로 ODA 상용 리버스엔지니어링 SDK를 감싼 것(소스 비공개 바이너리, ODA 풀라이선스 수만 유로).
### 6.2 변환 경로 (전부 서버측 배치, 프로젝트 생성 시)
| 입력 | 변환 경로 | 뷰어 로딩 포맷 | 비고 |
|---|---|---|---|
| **RVT** | rvt → IFC (DDC 오프라인 or 사내 Revit) → XKT | XKT (xeokit) | Revit IFC export 품질 이슈 존재, 검증 필요 |
| **DWG(3D)** | dwg → IFC/glTF (DDC/ODA) → XKT | XKT | |
| **DWG(2D 도면)** | dwg → 지오레퍼런싱 래스터(GeoTIFF) | Cesium imagery 오버레이 | 구글어스식 도면 얹기 |
| **IFC** | ifc → XKT (convert2xkt, 서버) | XKT | 런타임 파싱 금지(That Open 실패 원인) |
| **FBX(지형)** | fbx → glTF, .fbm 오쏘 KTX2 압축, .pos로 CRS | glTF (xeokit 메시) | 전 구간 24MB급이면 단일 로딩 |
| **NWD** | nwd → glTF(속성 포함, Navisworks 플러그인) | glTF | **선택**: 안 받아도 됨. 받으면 속성 살아있음(정정: 지오메트리는 테셀레이션 메시, 속성은 매핑되면 보존) |
### 6.3 XKT / 사전변환이 "가벼움"의 본체
- IFC→XKT: 예) IFCv4 186MB → 12MB XKT(~95% 압축). 서버에서 굽고 브라우저는 스트리밍만.
- **런타임 파싱 절대 금지.** 프로젝트 생성 시 백그라운드 배치로 변환 → 사용자는 변환을 못 느낌 → "열어도 렉" 원천 차단.
### 6.4 대용량 오쏘 분기
- 오쏘 텍스처 ≤ 수십 MB(현재 케이스): 단일 glTF + KTX2. 3D Tiles 불필요.
- 오쏘 대형(수백MB~GB): 지형을 **3D Tiles 타일셋**으로 굽기(텍스처 피라미드), 지도 모드에서 Cesium 스트리밍.
---
## 7. 뷰 퀄리티 (실사감) — 성능과 양립
- PBR 머티리얼·SAO(앰비언트 섀도)·환경광은 **근접 LOD에서만** 켠다. 전역으로 켜면 20km에서 프레임 죽음.
- 원거리 = 경량, 근접 = 풀디테일. 비주얼 퀄리티 ↔ 성능은 정면충돌하는 축이므로 LOD에 종속시킨다.
---
## 8. MIR_SMART 연동 (트윈 b형, 전 기능)
### 8.1 트윈 정의 확정
- 여기서 "디지털트윈"은 **실제같은 3D 뷰(트윈처럼 보이는 뷰)**를 뜻함. **IoT/센서/실시간 텔레메트리 아님.**
- 유형: **b형 = as-built 페더레이션 모델 + 오브젝트에 물량/진도/RFI 데이터 링크한 데이터 결합형.** (a형=실시간 텔레메트리 아님. IoT는 지금 구현 안 함, 훅만 남김.)
### 8.2 연동 방식
- **오브젝트 GUID ↔ MIR_SMART DB** 매핑.
- MIR_SMART 전 기능(물량 QTO·진도 대시보드·RFI) 오브젝트 클릭에서 접근.
- 필터·하이라이트·속성패널. 주로 BIM 검토 모드(xeokit)에서 작동.
- xeokit 메타데이터 API로 GUID·PropertySet 조회 → DB 조인.
---
## 9. 지도 기능 (지도 모드)
- **베이스맵:** VWorld(국토부, 공공·무료, 국내 좌표계 친화) 우선 검토. Cesium ion imagery도 가능.
  - **카카오맵은 필수 아님.** 카카오 타일을 3D 뷰 바닥에 까는 건 약관·좌표계(WCONGNAMUL/EPSG:3857 변환) 벽이 있어 뷰어 본체엔 부적합.
- **위성/지도 토글:** Cesium imagery layer 전환(스크린샷 우상단 그거).
- **CAD 도면 오버레이:** DWG→지오레퍼런싱 래스터→Cesium imagery로 바닥에 깔기 + on/off 토글.
---
## 10. 별개 모듈 — 현장 길찾기 (스코프 아웃)
- "현장지도 만들어 길찾기 연동"은 **본 뷰어와 분리된 별개 프로젝트.** (사용자 직감이 정확함.)
- BIM 트윈 뷰어 = 3D 정밀 검토 / 길찾기 = 2D 내비게이션 — 성격 완전히 다름. 한 뷰어에 합치면 둘 다 어설퍼짐.
- **길찾기는 카카오/네이버 지도 API로 별도 화면/모듈.** 트윈과는 **"프로젝트 위치 좌표 1개만 공유"**하는 느슨한 결합.
  - 예: 트윈에서 "현장 위치" 클릭 → 카카오맵 길찾기로 좌표 넘김.
- 카카오 라이선스·좌표 문제를 길찾기 모듈에만 국한 → 뷰어 본체 오염 방지.
---
## 11. 미해결 결정 & 리스크 (PoC/측정으로 확정할 것)
| # | 미결 항목 | 확정 방법 | 갈림 기준 |
|---|---|---|---|
| R1 | **가장 위험한 가정:** 60만대 실좌표 FBX 지형이 xeokit에서 지터 없이 가볍게 뜨는가 | **PoC 1 (§13)** | 뜨면 전체 설계 성립, 안 뜨면 재설계 |
| R2 | 구조물을 (a) xeokit을 Cesium에 합성 vs (b) 구조물도 3D Tiles로 통일 | 속성 상호작용 깊이로 PoC 판단 | MIR_SMART 오브젝트 클릭이 깊은 데이터 패널이면 (a), 단순 표시면 (b). 트윈 b형이라 (a) 유력 |
| R3 | "몇십배" 오쏘 케이스가 지형에 Cesium 3D Tiles를 강제하는가 | 대표 대형 현장 오쏘 용량 실측 | 수백MB~GB면 3D Tiles, 아니면 단일 glTF |
| R4 | Revit→IFC export 지오메트리/속성 품질(GUID 안정성 포함) | 실제 rvt 샘플 변환 검증 | 품질 낮으면 DDC vs 사내 Revit 경로 재선택 |
| R5 | 지도 모드 베이스: VWorld vs Cesium ion imagery | 국내 좌표 정합·라이선스 확인 | |
---
## 12. 개발 로드맵 (PoC 우선)
> **원칙: 가장 위험한 가정(R1)을 코드로 먼저 깬다. 아키텍처 토론은 끝. 검증 단계다.**
- **Phase 0 — PoC 1 (좌표 지터 검증):** §13. 이게 통과해야 나머지 의미 있음.
- **Phase 1 — 구조물 정합:** IFC 1개 → XKT → 같은 씬에 얹어 자동 정합 확인. (R2 방식 비교 시작.)
- **Phase 2 — 서버 인제스트 파이프라인:** 프로젝트 생성 배치(포맷별 변환 §6, .pos 파싱 §5, KTX2 압축).
- **Phase 3 — MIR_SMART 연동:** GUID↔DB, 속성패널·필터·하이라이트, 물량/진도/RFI(§8).
- **Phase 4 — 지도 모드:** Cesium 베이스 + 위성/지도 토글 + CAD 래스터 오버레이(§9). 모드 전환.
- **Phase 5 — 뷰 퀄리티 튜닝:** LOD 기반 PBR/SAO(§7), 대용량 오쏘 3D Tiles 분기(§6.4).
- **(별개) 길찾기 모듈:** §10. 본 프로젝트와 분리 진행.
---
## 13. PoC 1 상세 스펙 (즉시 착수)
**목표:** "중부원점 60만대 실좌표 FBX 지형을 xeokit에서 지터 없이 60fps로 렌더" 검증. (That Open 실패의 좌표 원인이 xeokit에서 소멸하는지 확인.)
**입력:** `ground_수정.fbx` + `ground_수정.fbm`(오쏘) + `ground_수정.pos`
**단계:**
1. FBX → glTF 변환 (`FBX2glTF` CLI 또는 Blender headless CLI). 텍스처 임베드 여부 확인(.fbm 외부참조 → glTF 임베드/복사).
2. `.pos` 파싱 → central_meridian/false_northing으로 EPSG 확정(→ 5186 예상). FBX 정점 bbox 검사로 실좌표(20만/60만대) 확인 = 시나리오 1.
3. xeokit Viewer 생성(`dtxEnabled: true`), glTF 로딩, **실좌표 그대로**(re-center 없이).
4. Stats.js로 FPS·메모리 측정. **카메라 조작 시 지터·z-fighting 없는지 육안 확인.**
5. 오쏘 텍스처 드래이핑 정상 표시 확인. (안 되면 KTX2 변환·UV 확인.)
**성공 기준:**
- 60만대 좌표에서 모델 떨림 없음.
- 인터랙션 60fps 유지.
- 오쏘가 지형에 정상 드래이핑.
**실패 시 대응:**
- 지터 발생 → xeokit 좌표 처리 옵션 재확인, 최악의 경우 프로젝트 원점 re-center 전략 도입.
- 성능 미달 → LOD/컬링/DTX 튜닝, 지형 메시 데시메이션.
---
## 14. 기술 스택 & 라이선스
| 영역 | 도구 | 라이선스 | 비고 |
|---|---|---|---|
| BIM 뷰어 엔진 | **xeokit SDK** | AGPLv3 | 사내 전용이라 OK. 외부 배포 시 재검토 |
| 지도 베이스 | **CesiumJS** | Apache 2.0 | 지도 모드 |
| IFC→XKT 변환 | xeokit `convert2xkt` / ifc2gltf(Creoox) | 오픈소스/일부 상용 | 서버 배치 |
| FBX→glTF | FBX2glTF / Blender CLI | 오픈소스 | 지형 |
| 텍스처 압축 | KTX2 / Basis Universal | 오픈소스 | 오쏘 성능 핵심 |
| 좌표 변환 | pyproj (PROJ) | MIT/오픈소스 | 5186↔WGS84, 지도 모드 |
| rvt/dwg 변환 | DataDrivenConstruction / ODA 기반 | 무료 바이너리(ODA 상용 래핑) | 폐쇄포맷 우회 |
| IFC 데이터 처리 | IfcOpenShell(필요 시) | LGPL | 속성 추출 백엔드 |
| 프론트 | TypeScript / Three.js(xeokit 내부) | | Claude Code 궁합 좋음 |
> **참고:** That Open(web-ifc/fragments)은 MIT라 라이선스는 더 자유롭지만 float32 지터 문제로 이 프로젝트엔 xeokit 채택. 만약 R1 PoC에서 xeokit이 예상과 다르면 fragments+re-center를 재검토.
---
## 15. Claude Code 운영 가이드
### 15.1 진행 방식
- **웹 + GitHub 연동**(claude.ai/code)으로 주 개발. 레포 클론 → claude/ 브랜치 작업 → PR. 세션 영구·모바일 모니터링.
- 무거운 로컬 변환/최종 검증은 `claude --teleport`로 웹 세션을 로컬로 끌어와 처리.
- 레포 루트 `CLAUDE.md`에 코딩 규칙·금지 디렉토리·테스트 방식 정의(웹/CLI 공통 로딩).
### 15.2 대용량 파일 처리 (중요)
- FBX/오쏘/변환 산출물은 **채팅 업로드 불가**(채팅은 스크린샷·PDF·다이어그램만). GitHub 레포로 관리.
- GitHub 파일 100MB 제한 → **Git LFS** 또는 별도 오브젝트 스토리지(S3/Supabase 등). 대형 오쏘·XKT는 LFS/스토리지에.
- PoC용 `ground_수정.fbx`(14MB)는 Git LFS로 커밋 가능. "몇십배" 케이스는 스토리지 버킷.
### 15.3 네트워크
- 클라우드 세션 기본 네트워크는 Trusted(패키지 레지스트리 등 허용). npm(xeokit/three/cesium 등) 설치 OK.
- 특정 호스트(변환 도구 다운로드, VWorld/Cesium 타일, Supabase 등) 필요 시 환경의 Allowed domains에 추가.
### 15.4 레포 구조(제안)
```
/
├─ CLAUDE.md                 # 코딩 규칙 + 이 브리프 핵심 요약
├─ PROJECT_BRIEF.md          # 이 문서
├─ /viewer                   # 프론트(xeokit + cesium 듀얼 모드)
├─ /ingest                   # 서버측 변환 파이프라인
│   ├─ pos_parser/           # .pos → EPSG 판별 (§5.2)
│   ├─ ifc2xkt/
│   ├─ fbx2gltf/
│   └─ ortho_ktx2/
├─ /integration              # MIR_SMART DB 연동(GUID↔레코드)
├─ /data (Git LFS)           # PoC 샘플: ground_수정.*
└─ /poc                      # Phase 0 PoC 1
```
---
## 16. 알려진 함정 체크리스트 (전 대화에서 도출)
- [ ] **런타임 IFC 파싱 금지** — 반드시 서버 사전변환(XKT). (That Open 실패 원인 #1)
- [ ] **float32 지터** — xeokit 더블 프리시전 사용. re-center는 최후수단. (실패 원인 #2)
- [ ] **한 화면에 다 켜지 않기** — 모드 분리로 가벼움 유지.
- [ ] **`.pos` 이름 문자열 믿지 않기** — central_meridian/false_northing으로 EPSG 판별.
- [ ] **`.pos` coordinates가 [0,0,0]** — FBX 정점 실좌표 여부 이중 확인(시나리오 1/2).
- [ ] **Revit 좌표계 자동판별 금지** — 프로젝트당 사용자 1회 지정.
- [ ] **오쏘 텍스처가 진짜 병목** — KTX2 압축 + 밉맵 + (대형 시)타일링. 지형 메시 아님.
- [ ] **Navisworks는 인제스트 병목** — 자동화 위해 경로에서 제외, `.pos` 직접 파싱.
- [ ] **PBR/SAO는 근접 LOD에서만** — 전역 사용 시 20km에서 프레임 죽음.
- [ ] **좌표 재투영은 지도 모드에만** — BIM 모드는 평면 TM 그대로.
- [ ] **길찾기는 별개 모듈** — 뷰어 본체에 합치지 않기.
- [ ] **rvt/nwd/dwg 순수 OSS 리딩 불가** 인지 — 변환 레이어(ODA 래핑) 의존 명시.
- [ ] **xeokit AGPL** — 사내 전용 유지 조건. 외부 배포 시 법무 재검토.
---
## 17. 다음 액션
1. GitHub 레포 생성, 이 문서 + `CLAUDE.md` 커밋, `ground_수정.*`를 Git LFS로 `/data`에 추가.
2. **Phase 0 / PoC 1 착수**(§13): FBX→glTF→xeokit 로딩 + 좌표 지터 검증. R1 확정.
3. PoC 1 결과에 따라 Phase 1(구조물 정합) 진행 및 R2(구조물 렌더 방식) 판단.
