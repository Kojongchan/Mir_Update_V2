# CLAUDE.md — MIR_SMART 자체 3D 뷰어

> 전체 맥락·근거·기각된 대안은 `PROJECT_BRIEF.md`가 단일 진실 소스다.
> 이 파일은 **매 세션 반드시 지켜야 할 핵심 규칙**만 압축한다. 결정이 바뀌면 두 파일 모두 갱신한다.

## 프로젝트 한 줄
ACC 뷰어(렉)를 대체하는 **사내 전용 웹 3D BIM 디지털트윈 뷰어**. 최우선 가치는 **"가벼움"**.

## 우선순위 (순서 = 우선순위, 절대 뒤집지 말 것)
1. 가벼운 자체 3D 뷰 (본질)
2. 뷰 퀄리티(실사감)
3. 다포맷 업로드→3D (rvt/dwg/ifc/fbx, 좌표 포함)
4. MIR_SMART 전 기능 연동 (물량/진도/RFI, 트윈 b형)
5. 지도 기능 (위성/지도 배경)

## 확정 아키텍처 (듀얼 엔진 / 모드 분리)
- **BIM 검토 모드 = xeokit SDK** (더블 프리시전 float64, 실좌표 네이티브). 주력. MIR_SMART 기능이 다 붙는 곳.
- **지도 모드 = CesiumJS** (위성/지도 배경, CAD 래스터 오버레이, 경량 구조물 프록시).
- 두 모드는 **카메라만 공유**. 한 씬에 전부 욱여넣지 않는다 → 가벼움의 핵심.

## 절대 규칙 (함정 체크리스트 — 위반 금지)
- [ ] **런타임 IFC 파싱 금지.** 반드시 서버 사전변환(IFC→XKT). (That Open 실패 원인 #1)
- [ ] **float32 지터 금지.** xeokit 더블 프리시전 사용, 실좌표 그대로. re-center는 최후수단. (실패 원인 #2)
- [ ] **한 화면에 다 켜지 않기.** 모드 분리로 가벼움 유지.
- [ ] **`.pos` 이름 문자열 믿지 않기.** `central_meridian` + `false_northing`으로 EPSG 판별.
- [ ] **`.pos` coordinates == [0,0,0]** 이면 FBX 정점 실좌표 여부 이중 확인(시나리오 1/2).
- [ ] **Revit 좌표계 자동판별 금지.** 프로젝트당 사용자가 1회 지정(드롭다운).
- [ ] **오쏘 텍스처가 진짜 병목.** KTX2 압축 + 밉맵 필수. 지형 메시가 아님.
- [ ] **PBR/SAO는 근접 LOD에서만.** 전역 사용 시 20km에서 프레임 죽음.
- [ ] **좌표 재투영(5186↔WGS84)은 지도 모드에서만.** BIM 모드는 평면 TM 그대로.
- [ ] **길찾기는 별개 모듈.** 뷰어 본체에 합치지 않기.
- [ ] **rvt/nwd/dwg는 폐쇄 포맷** — 순수 OSS 리딩 불가, 변환 레이어(ODA 래핑) 의존.
- [ ] **xeokit AGPLv3** — 사내 전용 유지 조건. 외부 배포/SaaS화 시 법무 재검토.

## EPSG 판별표 (한국 평면직각, `.pos` central_meridian 기준)
| central_meridian | 원점 | EPSG | false_northing |
|---|---|---|---|
| 125 | 서부 | 5185 | 600000 |
| 127 | 중부 | 5186 | 600000 |
| 129 | 동부 | 5187 | 600000 |
| 131 | 동해 | 5188 | 600000 |
- `false_northing == 500000`이면 구 좌표계(5174 계열) 의심 → 경고, 자동 확정 금지.

## 개발 로드맵 (PoC 우선 — R1 먼저 깬다)
- **Phase 0** — PoC 1: 60만대 실좌표 FBX 지형이 xeokit에서 지터 없이 60fps로 뜨는지 검증 (§13). ← 지금 여기.
- **Phase 1** — 구조물 정합 (IFC→XKT, 같은 씬 자동 정합).
- **Phase 2** — 서버 인제스트 파이프라인 (포맷 변환 + .pos 파싱 + KTX2).
- **Phase 3** — MIR_SMART 연동 (GUID↔DB, 속성/필터/하이라이트, 물량/진도/RFI).
- **Phase 4** — 지도 모드 (Cesium 베이스 + 토글 + CAD 오버레이).
- **Phase 5** — 뷰 퀄리티 튜닝 (LOD 기반 PBR/SAO, 대용량 오쏘 3D Tiles 분기).

## 레포 구조
```
/viewer         # 프론트 (xeokit + cesium 듀얼 모드)
/ingest         # 서버측 변환 파이프라인
  /pos_parser   # .pos → EPSG 판별 (§5.2) — 구현됨
  /ifc2xkt      # (예정)
  /fbx2gltf     # (예정)
  /ortho_ktx2   # (예정)
/integration    # MIR_SMART DB 연동 (GUID↔레코드)
/data           # PoC 샘플 (Git LFS): ground_수정.*
/poc            # Phase 0 PoC 1
```

## 코딩 규칙
- **언어:** 프론트 = TypeScript. 인제스트/좌표/속성 처리 = Python 3.11+.
- **대용량 파일**은 채팅 업로드 불가. GitHub + Git LFS(≤100MB) 또는 오브젝트 스토리지.
- **테스트:** Python은 `pytest` (표준 라이브러리만으로 실행 가능하게 유지). 커밋 전 통과 확인.
- 브랜치: `claude/*`에서 작업 → PR. 설계를 바꾸면 `PROJECT_BRIEF.md` 근거를 갱신하고 커밋.
- 결정에 확신이 없거나 브리프와 상충하면, 코드로 밀어붙이기 전에 브리프의 근거를 확인한다.
